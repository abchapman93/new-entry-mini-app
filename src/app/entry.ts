export class Entry {
  constructor(
    // public id: number,
    public date?: string,
    public severity?: number,
    public trigger?: string,
    public comments?: string
    // triggers
    // medications
  ) {}
}
